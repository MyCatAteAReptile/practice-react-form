import colors from './colors';

const borderSize = {
    standart: '3px',
};

const borders = {
    border: `${borderSize.standart} solid ${colors.border}`,
    borderFocus: `${borderSize.standart} solid ${colors.borderFocus}`,
    borderHover: `${borderSize.standart} solid ${colors.borderFocus}`,
};

export default borders;
